apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slak-matrix-migration.fullname" . }}-test-connection"
  labels:
    {{- include "slak-matrix-migration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slak-matrix-migration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
