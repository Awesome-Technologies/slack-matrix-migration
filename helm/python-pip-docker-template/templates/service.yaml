apiVersion: v1
kind: Service
metadata:
  name: {{ include "slak-matrix-migration.fullname" . }}
  labels:
    {{- include "slak-matrix-migration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slak-matrix-migration.selectorLabels" . | nindent 4 }}
